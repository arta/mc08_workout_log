#index-workouts
  -@workouts.each do |workout|
    %h2= link_to workout.date.strftime( "%A %b %d" ), workout
    %h3
      %span Workout:
      = workout.workout
    =link_to 'Edit', [:edit, workout]
    =link_to 'Delete', workout, method: :delete, data:{ confirm:'Are you sure?' }
